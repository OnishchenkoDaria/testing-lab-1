# Generated by Selenium IDE
import pytest
import time
import json
from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.common.action_chains import ActionChains
from selenium.webdriver.support import expected_conditions
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.common.keys import Keys
from selenium.webdriver.common.desired_capabilities import DesiredCapabilities

class TestChaninglanguage():
  def setup_method(self, method):
    self.driver = webdriver.Chrome()
    self.vars = {}
  
  def teardown_method(self, method):
    self.driver.quit()
  
  def test_chaninglanguage(self):
    self.driver.get("https://agro-yakist.com.ua/")
    self.driver.maximize_window()   
    self.driver.find_element(By.ID, "top").click()
    assert self.driver.find_element(By.CSS_SELECTOR, "p:nth-child(1)").text == "Безкоштовна доставка «Новою поштою» та «Укрпоштою» до відділення при замовленні від 4000 грн за умови повної передплати (вага посилки — до 30 кг)"
    self.driver.find_element(By.CSS_SELECTOR, ".lang").click()
    elements = self.driver.find_elements(By.ID, "top")
    assert len(elements) > 0
    assert self.driver.find_element(By.LINK_TEXT, "Русский").text == "Русский"
    self.driver.find_element(By.LINK_TEXT, "Русский").click()
    self.driver.find_element(By.CSS_SELECTOR, "html").click()
    assert self.driver.find_element(By.CSS_SELECTOR, "p:nth-child(1)").text == "Бесплатная доставка «Новой почтой» и «Укрпочтой» до отделения при заказе от 4000 грн при условии полной предоплаты (вес посылки — до 30 кг)"
  
